#server:
#  port: 8081
#
#spring:
#  application:
#    name: auth-service
#  datasource:
#    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/flashsale_auth}
#    username: ${SPRING_DATASOURCE_USERNAME:postgres}
#    password: ${SPRING_DATASOURCE_PASSWORD:postgres}
#    driver-class-name: org.postgresql.Driver
#    hikari:
#      maximum-pool-size: 20
#      minimum-idle: 5
#      idle-timeout: 30000
#      connection-timeout: 20000
#      max-lifetime: 1800000
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: false
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect
#        format_sql: false
#  data:
#    redis:
#      host: ${SPRING_REDIS_HOST:localhost}
#      port: ${SPRING_REDIS_PORT:6379}
#      password: ${SPRING_REDIS_PASSWORD:}
#      timeout: 2000ms
#
#jwt:
#  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
#  access-token-expiration-ms: 900000 # 15 minutes
#  refresh-token-expiration-ms: 604800000 # 7 days
#
#eureka:
#  instance:
#    prefer-ip-address: true
#    instance-id: ${spring.application.name}:${random.value}
#  client:
#    service-url:
#      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka/}
#    register-with-eureka: true
#    fetch-registry: true
#
#management:
#  endpoints:
#    web:
#      exposure:
#        include: health,info,metrics
#  endpoint:
#    health:
#      show-details: always
server:
  port: 8081

spring:
  application:
    name: auth-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres:5432/flashsale_auth}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      idle-timeout: 30000
      connection-timeout: 20000
      max-lifetime: 1800000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
  data:
    redis:
      host: ${SPRING_REDIS_HOST:localhost}
      port: ${SPRING_REDIS_PORT:6379}
      password: ${SPRING_REDIS_PASSWORD:}
      timeout: 2000ms

jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
  access-token-expiration-ms: 86400000 # 24 hours instead of 15 minutes
  refresh-token-expiration-ms: 604800000 # 7 days

eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${random.value}
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka/}
    register-with-eureka: true
    fetch-registry: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
app:
  admin:
    enabled: true
    email: admin@flashsale.com
    password: Admin@123
    first-name: Admin
    last-name: Kumar